PGHOST=127.0.0.1
PGPORT=5432
PGUSER=postgres
PGPASSWORD=password123
PGDATABASE=petualangan_kata
PORT=3001

# Base URL of the backend API, used by the frontend build.
# Leave empty for same-origin deployments (frontend and backend on the same domain).
# Set to the backend's full origin when frontend and backend are on different domains,
# e.g. VITE_API_BASE_URL=https://dgbl.tipfile.com
VITE_API_BASE_URL=
